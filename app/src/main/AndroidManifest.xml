<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.phoncoin.wallet">

    <!-- 🌐 Network access (node, explorer, transfers) -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- 🔋 Proof-of-Phone mining / background execution -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!-- ✅ Android 14+ : required when using foregroundServiceType="dataSync" -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- ✅ Android 13+ : required to display foreground service notifications -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 📷 Camera access for QR code scanning (addresses, payments) -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-feature
        android:name="android.hardware.camera"
        android:required="false" />

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.WALLET"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- 🔑 Main activity (singleTask, no recreation on rotation) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:screenOrientation="fullSensor">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ⚙️ Proof-of-Phone foreground service (mining, Android 14+ compliant) -->
        <service
            android:name=".feature.mining.PopMiningService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:permission="android.permission.FOREGROUND_SERVICE" />
    </application>

</manifest>
